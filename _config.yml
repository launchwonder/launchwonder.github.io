# General info
baseurl:     ""
url:         "http://launchwonder.com"
date_format: "%b %-d, %Y"

# Collections
collections_dir: courses
collections:
  algebra:
    title: Algebra
    output: true
  computerscience:
    title: Computer Science
    output: true

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: ""
      type: "algebra"
    values:
      layout: "content"
  - scope:
      path: ""
      type: "computerscience"
    values:
      layout: "content"

# Plugins
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
